import { Injectable } from '@angular/core';

export interface User1 {
    uid?: string;
    username?: string;
    password?: string;
    fullname?: string;
    address?: string;
    contact?: string;
    roles?: string;
}
